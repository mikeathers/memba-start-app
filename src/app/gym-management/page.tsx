import React from 'react'
import {GymManagement} from '@/app-pages'
import {gymManagementContent} from '@/content'

const GymManagementPage: React.FC = () => {
  return <GymManagement content={gymManagementContent} />
}

export default GymManagementPage
