import React from 'react'
import {Apps} from '@/app-pages'
import {appsContent} from '@/content'

const AppsPage: React.FC = () => {
  return <Apps content={appsContent} />
}

export default AppsPage
